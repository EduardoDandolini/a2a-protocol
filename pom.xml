<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>br.com.talk.a2a</groupId>
  <artifactId>quem-autorizou-esse-agente</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>Quem autorizou esse agente?</name>
  <description>
    Projeto de demonstracao da palestra "Quem autorizou esse agente?" - lacunas de governanca
    no protocolo A2A (Agent2Agent). Demo A e um agente de estorno A2A ingenuo; Demo B e o mesmo
    cenario com governanca aplicada (registry curado + cards assinados, autorizacao antes de
    aceitar a task, escopo atenuado a cada salto, idempotencia e compensacao).
  </description>

  <modules>
    <module>a2a-spring-support</module>
    <module>governance-common</module>
    <module>demo-a-naive-agent</module>
    <module>demo-b-governed</module>
  </modules>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <maven.compiler.release>21</maven.compiler.release>

    <spring-boot.version>3.5.16</spring-boot.version>

    <!-- SDK Java oficial do A2A -->
    <a2a.version>1.3.1.Final</a2a.version>

    <!-- LangChain4j "puro" (sem extensoes de framework) -->
    <langchain4j.version>1.20.0</langchain4j.version>

    <!-- ATENCAO: langchain4j-agentic / langchain4j-agentic-a2a estao em BETA.
         Conforme a propria palestra recomenda: "Diga voce mesmo que o modulo e beta." -->
    <langchain4j-agentic.version>1.20.0-beta30</langchain4j-agentic.version>

    <!-- Biblioteca JOSE usada na assinatura JWS demo do Agent Card e no token exchange -->
    <nimbus-jose-jwt.version>10.9.1</nimbus-jose-jwt.version>

    <compiler-plugin.version>3.16.0</compiler-plugin.version>
    <surefire-plugin.version>3.5.4</surefire-plugin.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <dependency>
        <groupId>org.a2aproject.sdk</groupId>
        <artifactId>a2a-java-sdk-bom</artifactId>
        <version>${a2a.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <dependency>
        <groupId>dev.langchain4j</groupId>
        <artifactId>langchain4j-bom</artifactId>
        <version>${langchain4j.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <dependency>
        <groupId>dev.langchain4j</groupId>
        <artifactId>langchain4j-agentic</artifactId>
        <version>${langchain4j-agentic.version}</version>
      </dependency>
      <dependency>
        <groupId>dev.langchain4j</groupId>
        <artifactId>langchain4j-agentic-a2a</artifactId>
        <version>${langchain4j-agentic.version}</version>
      </dependency>

      <dependency>
        <groupId>com.nimbusds</groupId>
        <artifactId>nimbus-jose-jwt</artifactId>
        <version>${nimbus-jose-jwt.version}</version>
      </dependency>

      <dependency>
        <groupId>br.com.talk.a2a</groupId>
        <artifactId>a2a-spring-support</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>br.com.talk.a2a</groupId>
        <artifactId>governance-common</artifactId>
        <version>${project.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${compiler-plugin.version}</version>
          <configuration>
            <parameters>true</parameters>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${surefire-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <version>${spring-boot.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
